# RideFlow frontend environment template
# In Vercel, set these as Environment Variables.
# For local Vite dev with proxy, these can stay unset.

VITE_API_BASE_URL=https://your-backend-domain.example.com
VITE_WS_BASE_URL=wss://your-backend-domain.example.com